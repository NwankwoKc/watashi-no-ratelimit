import { Request,Response,NextFunction } from "express";
import { Options } from "./type";

const limit = async (options:Options<string>) =>{
    (req:Request,res:Response,next:NextFunction)=>{
        
    }
}